import { IndexableTypeArray, PromiseExtended } from "dexie";
import { useLiveQuery } from "dexie-react-hooks";
import { db } from "../models/db";
import { Lift } from "../models/lift";

export default function PR_Collection() {
  return null;
}
